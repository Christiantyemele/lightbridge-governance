/target
**/*.rs.bk
.env
.env.*
.idea/
!.env.example
/config/tls
*.pem
.DS_Store

# `helm dependency update|build` materializes chart dependencies here as
# .tgz. Chart.lock is the committed source of truth; these are regenerated
# by the helm-oci-publish CI action (helm dependency build) and locally.
charts/*/charts/

# Byte-cache from scripts/generate_dashboards.py. The generator is a
# dev-time-only tool -- its OUTPUT (the dashboard JSON) is committed and is
# what the chart ships, so nothing at deploy or CI time runs Python at all.
__pycache__/
.kilo/
charts/redact-gateway/k3d-values.yaml
